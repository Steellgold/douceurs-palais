  {% if variant == 'small' %}
    <a href="{{ href }}" class="relative rounded-lg overflow-hidden group h-40 shadow-md">
      <div class="absolute inset-0 bg-gray-800 opacity-50 group-hover:opacity-40 transition-opacity z-10"></div>
      {% if imageUrl %}
        <img src="/images/{{ imageUrl }}" alt="{{ name }}" class="w-full h-full object-cover" />
      {% else %}
        <div class="w-full h-full flex items-center justify-center bg-gray-300">
          <i data-lucide="bar-chart" class="h-10 w-10 text-gray-400"></i>
        </div>
      {% endif %}
      <div class="absolute inset-0 flex flex-col justify-end p-3 z-20">
        <div class="flex items-center justify-between">
          <h3 class="text-base font-playfair text-white">{{ name }}</h3>
          {% if productCount is not null %}
            <span class="text-[10px] font-medium bg-white text-gray-800 px-2 py-0.5 rounded-full">{{ productCount }}</span>
          {% endif %}
        </div>
        {% if description %}
          <p class="text-white/80 text-xs mt-1 line-clamp-2">{{ description }}</p>
        {% endif %}
      </div>
    </a>
{% else %}
  <a href="{{ href }}" class="relative rounded-lg overflow-hidden group h-64 shadow-lg">
    <div class="absolute inset-0 bg-gray-800 opacity-50 group-hover:opacity-40 transition-opacity"></div>
    {% if imageUrl %}
      <img src="/images/{{ imageUrl }}" alt="{{ name }}" class="w-full h-full object-cover" />
    {% else %}
      <div class="w-full h-full flex items-center justify-center bg-gray-300">
        <i data-lucide="bar-chart" class="h-16 w-16 text-gray-400"></i>
      </div>
    {% endif %}
    <div class="absolute inset-0 flex flex-col justify-end p-6">
      <div class="flex items-center justify-between">
        <h3 class="text-2xl font-playfair text-white">{{ name }}</h3>
        {% if productCount is not null %}
          <span class="text-sm font-medium bg-white text-gray-800 px-2 py-1 rounded-full">{{ productCount }} produits</span>
        {% endif %}
      </div>
      {% if description %}
        <p class="text-white/80 text-sm mt-2 line-clamp-2">{{ description }}</p>
      {% endif %}
      <div class="mt-4 opacity-0 group-hover:opacity-100 transition-opacity">
        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-white text-gray-800">
          Voir les produits
          <i data-lucide="arrow-right" class="ml-1 h-4 w-4"></i>
        </span>
      </div>
    </div>
  </a>
{% endif %}